name: Release Duely

on:
  pull_request:
    branches: [master]
    types: [closed]

permissions:
  contents: read

jobs:
  release:
    if: github.event.pull_request.merged == true
    uses: ./.github/workflows/release-component.yml
    with:
      component: duely
      build: true
      build_dir: Backend/Duely/ansible/build
      deploy_dir: Backend/Duely/ansible/deploy
      vault: true
    secrets: inherit
